/**
 * 性别枚举
 */
export enum Gender {
  MALE = 'male',
  FEMALE = 'female'
}

/**
 * 用户状态枚举（扩展用）
 */
export enum UserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended'
}

/**
 * 用户类型枚举（扩展用）
 */
export enum UserType {
  REGULAR = 'regular',
  PREMIUM = 'premium',
  VIP = 'vip'
}
